import { useEffect, useState } from "react";
import { supabase } from "../../lib/supabase";
import { SuperadminShell } from "./SuperadminShell";

export function SuperadminPlansPage() { const [plans,setPlans]=useState<any[]>([]); useEffect(()=>{supabase.from("subscription_plans").select("*").order("monthly_price").then(({data})=>setPlans(data??[]));},[]); return <SuperadminShell title="Planes" description="Catálogo comercial SaaS de Medin."><div className="grid gap-4 lg:grid-cols-5">{plans.map(plan=><div key={plan.id} className="rounded-lg border border-clinic-line bg-white p-4"><p className="font-semibold">{plan.name}</p><p className="mt-2 text-xl font-semibold">${Number(plan.monthly_price).toLocaleString("es-AR")}</p><p className="text-sm text-clinic-muted">Setup ${Number(plan.setup_price??0).toLocaleString("es-AR")}</p><p className="mt-3 text-sm">{plan.max_professionals??"∞"} profesionales · {plan.max_users??"∞"} usuarios</p><p className="mt-2 text-xs text-clinic-muted">{plan.active?"Activo":"Inactivo"}{plan.recommended?" · Recomendado":""}</p></div>)}</div></SuperadminShell>; }
export function SuperadminSubscriptionsPage() { const [subscriptions,setSubscriptions]=useState<any[]>([]); useEffect(()=>{supabase.from("clinic_subscriptions").select("*, clinics(name,slug), subscription_plans(name)").order("updated_at",{ascending:false}).then(({data})=>setSubscriptions(data??[]));},[]); return <SuperadminShell title="Suscripciones" description="Estado comercial, vencimientos y plan actual por clínica."><div className="overflow-hidden rounded-lg border border-clinic-line bg-white">{subscriptions.map(item=><div key={item.id} className="grid gap-2 border-b border-clinic-line px-4 py-3 md:grid-cols-4"><p className="font-semibold">{item.clinics?.name}</p><p>{item.subscription_plans?.name??"Sin plan"}</p><p className="text-sm text-clinic-muted">{item.status}</p><p className="text-sm text-clinic-muted">Vence: {item.current_period_end?new Date(item.current_period_end).toLocaleDateString("es-AR"):"A confirmar"}</p></div>)}{!subscriptions.length&&<p className="p-6 text-sm text-clinic-muted">Sin suscripciones.</p>}</div></SuperadminShell>; }
